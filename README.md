# Запуск сервиса
1. Для того, чтобы запустить сервис, необходимо клонировать репозиторий `git@github.com:IPerkhun/infocard-studio.git`
2. Далее, написать команду `uv run sync`. Все необходимые библиотеки с корректными версиями сами установятся 
3. Вводим доп команду pip install git+https://github.com/huggingface/diffusers
4. uv run app.py
```